config-version: 2

name: 'dbt_cortex_agent_integration_tests'
version: '0.1.0'
require-dbt-version: [">=1.5.0", "<3.0.0"]

profile: 'dbt_cortex_agent'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

seeds:
  dbt_cortex_agent_integration_tests:
    +quote_columns: false
